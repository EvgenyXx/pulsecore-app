<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Консоль -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Файл для Masters API -->
    <appender name="MASTERS_API_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/masters-api.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/masters-api.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Masters API логи только в файл, не в консоль -->
    <logger name="ru.pulsecore.app.tournament.infrastructure.client.MastersApiClient" level="WARN" additivity="false">
        <appender-ref ref="MASTERS_API_FILE"/>
    </logger>

    <!-- Остальное в консоль -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>